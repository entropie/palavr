%h1= @category.title
.backlink
  &larr;
  %a{:class => "awesome small silver", :href => BoardController.r} Back to Catgories
  &crarr;
  %a{:class => "awesome small silver", :href => BoardController.r(:create, :category => @category.id)} Create
#threads
  - @threads.each do |thread|
    = render_file "view/thread/_thread.haml", :phread => thread
