%tr
  %td.main
    .add
      .info{:title => "#{@cat.phreads.size} Stories"}= @cat.phreads.size
    .title
      %a{:href => BoardController.r(@cat.url)}= @cat.title
    .img
      %img{:src => "/img/nopic.png", :height => 60, :width => 60}
    .desc
      = @cat.description
  %td{:class => :modlist}= @cat.mods.map(&:email).join(", ")
  
